<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #f0f0f0;
        }

        .image-container {
            position: relative;
        }

        #image-to-map {
            max-width: 100%;
        }

        [data-area]:hover::before {
            content: attr(data-area);
            position: fixed;
            top: 20px; /* Adjusted to 20px from the top */
            left: 50%;
            transform: translateX(-50%);
            padding: 8px;
            background-color: #333;
            color: #fff;
            border-radius: 4px;
            opacity: 0; /* Start with 0 opacity */
            transition: opacity 0.3s ease-in-out;
        }

            [data-area]:hover::before {
                opacity: 1;
            }
        
       
    </style>
    
    <title>Hover Layout Based on Vegetable Map</title>
</head>

<body>
    <div class="image-container">
        <img src="/lib/vegetables.jpg" alt="Vegetable Map" usemap="#vegetableMap" id="image-to-map">
        <map name="vegetableMap">
            <area shape="circle" coords="300,300,200" alt="Tomato" data-area="Tomato" class="area-highlight">
            <area shape="circle" coords="800,200,200" alt="Lettuce" data-area="Lettuce" class="area-highlight">
            <area shape="circle" coords="1200,500,180" alt="Onion" data-area="Onion" class="area-highlight">
            <area shape="circle" coords="600,800,170" alt="Cucumber" data-area="Cucumber" class="area-highlight">
            <area shape="circle" coords="1500,800,160" alt="Potato" data-area="Potato" class="area-highlight">
        </map>
    </div>
        <div id="hover-tab"></div>    

    <script>
        //Create a List where a Query Selector will get all areas by class="area-highlight"
        let areaList = document.querySelectorAll('.area-highlight');
        //Create a loop that can iterate thoughout the list of areas
        for (let i=0; i<areaList.length; i++){
            //Add mouseover Events for dynamic switch between areas
            areaList[i].addEventListener('mouseover', (e) => {
                // Get the value of the 'data-area' attribute from the clicked element
                let textArea = e.target.getAttribute("data-area");
                // Find the element with the id 'hover-tab' and set its text content to the value of textArea
                document.getElementById('hover-tab').textContent = textArea;
        });
        }

            /*Explanation:

            e.target: Refers to the element that triggered the event.
            
            .getAttribute("data-area"): Gets the value of the 'data-area' attribute from the clicked element.
            
            let textArea: Declares a variable named textArea to store the value retrieved from the 'data-area' attribute.
            
            document.getElementById('hover-tab'): Finds the HTML element with the id 'hover-tab'.
        
            .textContent = textArea: Sets the text content of the found element to the value of textArea.
        */

    </script>
</body>

</html>
